torch>=2.2.0
transformers>=4.40.0
trl==0.8.6
datasets
accelerate
bitsandbytes
peft
vllm
# Unsloth is typically installed via a specific pip command depending on CUDA version, 
# but we will list the base requirement here for tracking.
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git